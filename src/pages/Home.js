import React, {useState, useEffect } from ÒreactÓ;import axios from ÒaxiosÓ;const Home = () => {	const [image, setImage] = useState([]);	useEffect(() => {		axios.get(Ò/api/imageÓ).then((response) => {			setImage(response.data);		});	}, []); 	return (		<div>			<h2>Image Gallery</h2>			{image.map((image) => (				<img key={image.id} src={image.url} alt={image.name} />			))}		</div>	);};Export default Home;